"""The ICP bot's account-fit scoring, copied from icp-bot/ (a read-only reference repo).

See compat.py for how its settings map onto ours, and pipeline.py for the per-company run
(adapted from its orchestrator.run()).
"""
