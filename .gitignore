# secrets
.env
*.env
!.env.example

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# local databases
*.db
*.sqlite

# node and the frontend build (built into api/static by npm run build and the Dockerfile)
node_modules/
dist/
api/static/

# ICP bot: a read-only reference clone of its own repo. Read it, never edit or commit it.
icp-bot/
